= simple_form_for(@answer) do |f|
  = f.error_notification

  .form-inputs
    = f.association :user
    = f.association :question
    = f.input :body
    = f.input :category

  .form-actions
    = f.button :submit
