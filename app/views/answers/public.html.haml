%h1 Listing answers

%table
  %thead
    %tr
      %th Question
      %th User
      %th Body
      %th Category
      %th
      %th
      %th

  %tbody
    - @answers.each do |answer|
      %tr
        %td= image_tag answer.question.image.url()
        %td= answer.user.email
        %td= answer.body
        %td= answer.category
        %td= link_to 'Show', [answer.question, answer]

= paginate @answers
