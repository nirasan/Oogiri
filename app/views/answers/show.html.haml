%p#notice= notice

.row
  = render @answer

- if @answer.user == current_user then
  = link_to 'Edit', edit_question_answer_path(@question, @answer)
  \|
  = link_to 'Destroy', [@question, @answer], :method => :delete, :data => { :confirm => 'Are you sure?' }

%h2 Comments

- if my_comment = @answer.comments.find_by(user:current_user) then
  = render my_comment
- else
  %div= link_to 'コメントする', new_question_answer_comment_path(@question, @answer)

- @answer.comments.where.not(user: current_user).each do |comment|
  = render comment
