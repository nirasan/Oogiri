- if current_user.favoriting?(user)
  = link_to 'お気に入り登録を解除する', favorite_path(current_user.favorites.find_by(to_user: user)), method: :delete, remote: true
- else
  = link_to 'お気に入りに登録する', favorites_path(to_user_id: user.id), method: :post, remote: true
